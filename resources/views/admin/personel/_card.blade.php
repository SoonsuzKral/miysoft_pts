{{-- Generated by Cursor prompt — module: Personel — date: 2026-03-15 --}}
<div class="bg-white rounded-xl border border-gray-100 shadow-sm overflow-hidden">

    {{-- Kart Başlığı --}}
    <div class="bg-gradient-to-r from-gray-900 to-gray-800 px-6 py-8 relative">
        <button onclick="document.getElementById('personelCardArea').classList.add('hidden')"
            class="absolute top-4 right-4 text-gray-400 hover:text-white">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
        </button>
        <div class="flex items-center gap-5">
            <div class="w-20 h-20 rounded-full bg-[#02E0FB] flex items-center justify-center text-white text-3xl font-bold shadow-lg">
                {{ strtoupper(substr($personel->first_name, 0, 1) . substr($personel->last_name, 0, 1)) }}
            </div>
            <div>
                <h2 class="text-2xl font-bold text-white">{{ $personel->full_name }}</h2>
                <p class="text-[#02E0FB] font-medium mt-0.5">{{ $personel->position?->title ?? 'Pozisyon Belirtilmemiş' }}</p>
                <p class="text-gray-400 text-sm mt-0.5">{{ $personel->department?->name ?? 'Departman Belirtilmemiş' }}</p>
                <div class="mt-2">
                    @if($personel->status === 'active')
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-500/20 text-green-400">Aktif</span>
                    @elseif($personel->status === 'on_leave')
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-500/20 text-yellow-400">İzinde</span>
                    @else
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-500/20 text-red-400">{{ ucfirst($personel->status) }}</span>
                    @endif
                </div>
            </div>
        </div>
    </div>

    {{-- Tab Navigasyon --}}
    <div class="border-b border-gray-100 px-6" x-data="{ activeTab: 'info' }">
        <div class="flex gap-4 -mb-px">
            @foreach([['info','Genel Bilgiler'],['leaves','İzinler'],['attendance','Puantaj'],['assets','Zimmetler'],['docs','Belgeler']] as [$tab, $label])
            <button @click="activeTab = '{{ $tab }}'"
                :class="activeTab === '{{ $tab }}' ? 'border-[#02E0FB] text-[#02E0FB]' : 'border-transparent text-gray-500 hover:text-gray-700'"
                class="py-3 px-1 text-sm font-medium border-b-2 transition-colors whitespace-nowrap">
                {{ $label }}
            </button>
            @endforeach
        </div>
    </div>

    {{-- Tab İçerikleri --}}
    <div class="p-6" x-data="{ activeTab: 'info' }">

        {{-- Genel Bilgiler Tab --}}
        <div x-show="activeTab === 'info'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="space-y-4">
                <h3 class="font-semibold text-gray-800 text-sm uppercase tracking-wider">İletişim Bilgileri</h3>
                <div class="space-y-3">
                    <div class="flex items-center gap-3 text-sm">
                        <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
                        <span class="text-gray-600">{{ $personel->email ?? '—' }}</span>
                    </div>
                    <div class="flex items-center gap-3 text-sm">
                        <svg class="w-4 h-4 text-gray-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                        <span class="text-gray-600">{{ $personel->phone ?? '—' }}</span>
                    </div>
                </div>
            </div>

            <div class="space-y-4">
                <h3 class="font-semibold text-gray-800 text-sm uppercase tracking-wider">İş Bilgileri</h3>
                <div class="space-y-3 text-sm">
                    <div class="flex justify-between">
                        <span class="text-gray-400">İşe Giriş</span>
                        <span class="text-gray-700 font-medium">{{ $personel->hire_date?->format('d.m.Y') ?? '—' }}</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-400">Kıdem</span>
                        <span class="text-gray-700 font-medium">
                            {{ $personel->hire_date ? $personel->hire_date->diffForHumans(now(), ['parts' => 2, 'short' => false]) : '—' }}
                        </span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-400">Doğum Tarihi</span>
                        <span class="text-gray-700 font-medium">{{ $personel->birth_date?->format('d.m.Y') ?? '—' }}</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-400">Kan Grubu</span>
                        <span class="text-gray-700 font-medium">{{ $personel->blood_type ?? '—' }}</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-400">TC Kimlik</span>
                        <span class="text-gray-700 font-medium font-mono">{{ $personel->masked_national_id }}</span>
                    </div>
                </div>
            </div>
        </div>

        {{-- İzin Özeti Tab --}}
        <div x-show="activeTab === 'leaves'">
            @if($personel->leaveRequests && $personel->leaveRequests->count() > 0)
                <div class="space-y-3">
                    @foreach($personel->leaveRequests->take(10) as $leave)
                    <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg text-sm">
                        <div>
                            <p class="font-medium text-gray-700">{{ $leave->leaveType?->name ?? 'İzin' }}</p>
                            <p class="text-xs text-gray-400">{{ $leave->start_date?->format('d.m.Y') }} - {{ $leave->end_date?->format('d.m.Y') }}</p>
                        </div>
                        <div class="flex items-center gap-3">
                            <span class="text-xs font-medium text-gray-600">{{ $leave->total_days }} gün</span>
                            <span class="inline-flex px-2 py-0.5 text-xs rounded-full
                                {{ $leave->status === 'approved' ? 'bg-green-100 text-green-700' : ($leave->status === 'rejected' ? 'bg-red-100 text-red-700' : 'bg-yellow-100 text-yellow-700') }}">
                                {{ ['pending'=>'Bekliyor','approved'=>'Onaylandı','rejected'=>'Reddedildi','cancelled'=>'İptal'][$leave->status] ?? $leave->status }}
                            </span>
                        </div>
                    </div>
                    @endforeach
                </div>
            @else
                <p class="text-center text-gray-400 py-8 text-sm">İzin kaydı bulunamadı</p>
            @endif
        </div>

        {{-- Puantaj Tab --}}
        <div x-show="activeTab === 'attendance'">
            <p class="text-center text-gray-400 py-8 text-sm">Son puantaj kayıtları yükleniyor...</p>
        </div>

        {{-- Zimmet Tab --}}
        <div x-show="activeTab === 'assets'">
            <p class="text-center text-gray-400 py-8 text-sm">Zimmet kaydı bulunamadı</p>
        </div>

        {{-- Belgeler Tab --}}
        <div x-show="activeTab === 'docs'">
            @if(isset($personel->documents) && $personel->documents->count() > 0)
                <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                    @foreach($personel->documents as $doc)
                    <div class="p-3 bg-gray-50 rounded-lg border border-gray-100 flex items-center gap-3 text-sm">
                        <svg class="w-8 h-8 text-[#02E0FB] shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
                        <div class="min-w-0">
                            <p class="font-medium text-gray-700 truncate">{{ $doc->type }}</p>
                            <p class="text-xs text-gray-400">{{ $doc->created_at?->format('d.m.Y') }}</p>
                        </div>
                    </div>
                    @endforeach
                </div>
            @else
                <p class="text-center text-gray-400 py-8 text-sm">Belge bulunamadı</p>
            @endif
        </div>
    </div>

    {{-- Kart Footer --}}
    <div class="px-6 py-4 border-t border-gray-100 flex justify-end gap-2">
        @can('personel.update')
        <button onclick="openEditModal({{ $personel->id }})" class="px-4 py-2 text-sm text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg transition-colors">
            Düzenle
        </button>
        @endcan
        @can('personel.export')
        <button onclick="axios.get('/admin/personel/{{ $personel->id }}/export-pdf').then(r => window.open(r.data.url))" class="px-4 py-2 text-sm text-white bg-[#FA6001] hover:bg-orange-500 rounded-lg transition-colors">
            PDF İndir
        </button>
        @endcan
    </div>
</div>
