{{-- Generated by Cursor prompt — module: Departman — date: 2026-03-15 --}}
<form id="deptForm" class="space-y-4" novalidate>
    @csrf

    {{-- Departman Adı --}}
    <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Departman Adı <span class="text-red-500">*</span></label>
        <input type="text" name="name" value="{{ $department->name ?? '' }}" required
            class="w-full px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:border-[#02E0FB] focus:ring-1 focus:ring-[#02E0FB]"
            placeholder="Örn: İnsan Kaynakları">
    </div>

    {{-- Kod --}}
    <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Departman Kodu</label>
        <input type="text" name="code" value="{{ $department->code ?? '' }}"
            class="w-full px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:border-[#02E0FB] font-mono"
            placeholder="IK" maxlength="10">
        <p class="mt-1 text-xs text-gray-400">Kısa tanımlayıcı (opsiyonel)</p>
    </div>

    {{-- Üst Departman --}}
    <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Üst Departman</label>
        <select name="parent_department_id" class="w-full px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:border-[#02E0FB]">
            <option value="">— Kök departman (üst yok) —</option>
            @foreach($departments ?? [] as $dept)
                <option value="{{ $dept->id }}" {{ isset($department) && $department->parent_department_id == $dept->id ? 'selected' : '' }}>
                    {{ str_repeat('⤷ ', $dept->depth ?? 0) }}{{ $dept->name }}
                </option>
            @endforeach
        </select>
    </div>

    {{-- Departman Yöneticisi --}}
    <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Departman Yöneticisi</label>
        <select name="manager_id" class="w-full px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:border-[#02E0FB]">
            <option value="">— Yönetici seçin —</option>
            @foreach($personels ?? [] as $p)
                <option value="{{ $p->id }}" {{ isset($department) && $department->manager_id == $p->id ? 'selected' : '' }}>
                    {{ $p->first_name }} {{ $p->last_name }}
                </option>
            @endforeach
        </select>
    </div>

    {{-- Durum --}}
    <div class="flex items-center gap-3">
        <label class="relative inline-flex items-center cursor-pointer">
            <input type="checkbox" name="is_active" value="1" class="sr-only peer"
                {{ !isset($department) || $department->is_active ? 'checked' : '' }}>
            <div class="w-11 h-6 bg-gray-200 rounded-full peer peer-checked:bg-[#02E0FB] transition-colors after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-full"></div>
        </label>
        <span class="text-sm text-gray-700">Aktif departman</span>
    </div>
</form>
